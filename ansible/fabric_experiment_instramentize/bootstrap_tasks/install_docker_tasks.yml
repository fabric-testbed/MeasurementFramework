---
# Installs the Docker.
- name: Install Docker 
  become: yes
  include_role: 
    name: geerlingguy.docker

# Installs Python Docker SDK

- name: Install Python Docker SDK
  pip:
    name: docker
    executable: pip3 

