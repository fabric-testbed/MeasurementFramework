[Unit]
Description=Synchronize system clock or PTP hardware clock (PHC)
Documentation=man:phc2sys

[Service]
Type=simple
ExecStart=/usr/sbin/phc2sys -E linreg -O +0 -s /dev/ptp_kvm -c {{ IFACE }}

[Install]
WantedBy=multi-user.target
