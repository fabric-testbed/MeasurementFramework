---
# Ping each experimentors node.
{% for h in groups["Experiment_Nodes"] %}
- targets:

        - '{{ hostvars[h].hostname }}'

  labels:
        job: ping
        fr: '{{ h }}'
{% endfor %}          




